# --- Web server ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # required by FastAPI for multipart file uploads

# --- srh-ss26-iot-project pipeline deps ---------------------------------
# The API imports extract_step/audio/video_features from the srh repo (see
# pipeline.py). Those functions need these installed in THIS environment.
# Versions mirror srh-ss26-iot-project/pyproject.toml.
numpy>=2.4.6
pandas>=3.0.3
scipy>=1.14                 # step features: butter/filtfilt/find_peaks/welch
librosa>=0.11.0             # audio features
soundfile>=0.14.0           # librosa wav backend
opencv-python>=4.13.0.92    # video decoding
mediapipe>=0.10.35          # mouth-opening features (needs models/face_landmarker.task in the srh repo)

# --- Dev / testing ---
httpx>=0.27                 # used by the end-to-end smoke test (test_flow.py)
